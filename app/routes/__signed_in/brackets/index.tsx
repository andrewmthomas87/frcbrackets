import { Typography } from "@mui/material";

export default function AboutTab(): JSX.Element {
  return (
    <Typography variant="body1" gutterBottom>
      Submit predictions for individual divisions or Einstein to participate in
      individual leaderboards. Submit predictions for everything to participate
      in the overall leaderboard.
    </Typography>
  );
}
